package ch4;

public class FlowEx19 {
    public static void main(String[] args) {
        for (int i = 1; i < 3; i++)
            for (int j = 1; j < 3; j++)
                for (int k = 1; k < 3; k++)
                    System.out.println("" + i + j + k);
    }

}

/*
 * System.out.println(""+i+j+k);에서 ""을 붙이는 이유는 문자열과 숫자 값을 결합하여 출력하기 위해서 입니다.
 * 
 * Java에서는 문자열과 다른 데이터 타입을 결합하면 문자열 타입으로 자동 형변환이 이루어지기 때문에, 문자열과 숫자 값을 결합하려면
 * 문자열로 변환해야 합니다.
 * 
 * 위 코드에서는 i, j, k가 int 타입으로 선언되어 있기 때문에, + 연산자를 사용하여 이들을 더한 후 문자열과 결합하면 자동으로
 * 문자열 타입으로 형변환이 이루어집니다. 따라서, ""+i+j+k는 i, j, k의 값을 더한 후 문자열로 변환하여 출력하는 것입니다.
 * 
 * 결과적으로, 위 코드는 "111", "112", "121", "122", "211", "212", "221", "222"와 같이
 * 출력됩니다.
 */
